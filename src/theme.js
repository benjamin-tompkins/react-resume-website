import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    background: {
      default: '#000015',
      paper: '#000015'
    },
    mode: 'dark',
    primary: {
      main: '#029ee8'
    },
    secondary: {
      main: '#101c26'
    },
  },
  
})